const largeur_base_aleatoire = aleatoire_entier(1, 15)
dessiner_pyramide(largeur_base_aleatoire)

function dessiner_pyramide(largeur_base) {
    let x = 50
    let y = 50
    let largeur = 50

    // La fonction triangle() est à coder
    triangle(x,y,largeur)
}
